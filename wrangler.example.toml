name = "alex-portfolio"
compatibility_date = "2026-06-03"

[[d1_databases]]
binding = "DB"
database_name = "alex-portfolio-db"
database_id = "replace-with-your-d1-database-id"

# Reference only. Do not put real account IDs, database IDs or secrets here.
# ADMIN_API_TOKEN must be configured as a Cloudflare secret/environment variable, not as a VITE variable.
