name = "alex-portfolio"
compatibility_date = "2026-01-01"

[[d1_databases]]
binding = "DB"
database_name = "alex-portfolio-db"
database_id = "replace-with-your-d1-database-id"

[vars]
BACKEND_ENV = "production"

# ADMIN_API_TOKEN must be configured as a secret, not as a public frontend variable.
